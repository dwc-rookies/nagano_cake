      <div class="jumbotron">
        <div class="container">
          
          <div class="row">
            <div class="col-xs-9">
              <div class="head1 container">
                <h1>Welcome to Naganocake</h1>
              </div> <br>
              このサイトは、ケーキ販売のECサイトになります。<br>
              会員でない方も商品の閲覧はできますが、<br>
              購入には会員登録が必要になります。<br>
            </div>
          </div>
          </div>


        <div class="recommend container">
          <h3>Recommend</h3>
          <div class="row">
            <div class="cols-3">
              <table>
                <tr>
                  <th>ジャンル検索</th>
                </tr>
                <% @genres.each do |genre| %>
                <tr>
                  <td>
                    <%= link_to products_path(genre_id: genre.id) do %>
                    <%= genre.name %>
                    <% end %>
                  </td>
                </tr>
                <% end %>
              </table>
            </div>
            <% @products.each do |product| %>
            <div class="cols-3 col-sm-3 col-xs-6 recommend-products">
              <!-- 画像をクリックしたら商品詳細画面へ遷移 -->
              <%= link_to product_path(product) do %>
              <%= attachment_image_tag product, :image, size: "150x150",  fallback: "no_image.jpg"%><br>
              <% end %>
              <p><%= product.name %></p>
              <span class="offset-8"><%= (product.tax_excluded_price*1.1).floor %> </span>
            </div>
            <% end %>
          </div>
        </div>
      </div>



      <div class="row">
        <div class="col-xs-9"></div>
        <div class="col-xs-1">
          <p><%= link_to "もっと見る＞", products_path %></p>
        </div>
      </div>
      </div>
      </div>